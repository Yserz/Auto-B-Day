<?xml version="1.0" encoding="UTF-8"?>
<!-- You may freely edit this file. See commented blocks below for -->
<!-- some examples of how to customize the build. -->
<!-- (If you delete it and reopen the project it will be recreated.) -->
<!-- By default, only the Clean and Build commands use this build script. -->
<!-- Commands such as Run, Debug, and Test only use this build script if -->
<!-- the Compile on Save feature is turned off for the project. -->
<!-- You can turn off the Compile on Save (or Deploy on Save) setting -->
<!-- in the project's Project Properties dialog box.-->
<project name="Auto-B-Day-war" default="default" basedir=".">
    <description>Builds, tests, and runs the project Auto-B-Day-war.</description>
    <!--<import file="nbproject/build-impl.xml"/>-->
	<description>
	    Beispiel eines Ant-Builds zur Erstellung eines JUnit-Test und Report
	</description>

	<property file="build.properties" />

	<path id="junit.classpath">
		<fileset dir="${war.lib.dir}">
			<include name="**/*.jar" />
		</fileset>
	</path>

	<target name="default" depends="clean, init, compile, test, cob-rep">
	</target>
	
	<target name="init">
		<mkdir dir="${war.classes.dir}" />
		<mkdir dir="${war.reports.xml.dir}" />
		<mkdir dir="${war.reports.html.dir}" />
		<mkdir dir="${war.inst.dir}" />
		<mkdir dir="${war.cob.rep.html}" />
		<mkdir dir="${war.cob.rep.xml}" />
		<mkdir dir="${war.cs.rep.xml}" />
		<mkdir dir="${war.cs.rep.html}" />
	</target>

	<target name="compile" depends="init">
		<javac srcdir="${war.src.dir}" destdir="${war.classes.dir}" debug="yes">
			<classpath refid="junit.classpath" />
		</javac>
		<javac srcdir="${war.test.dir}" destdir="${war.classes.dir}" debug="yes">
			<classpath refid="junit.classpath" />
		</javac>
	</target>

	<target name="test" depends="compile">
		 <junit errorProperty="test.failed" failureProperty="test.failed" fork="yes">

		 	<sysproperty key="net.sourceforge.cobertura.datafile" file="${war.cobdata}" />
		 	<classpath location="${war.inst.dir}" />

			<classpath location="${war.classes.dir}" />
			<classpath refid="junit.classpath" />

		 	<formatter type="brief" usefile="false"/>
			<formatter type="xml" />
		 	<test name="${testcase}" todir="${war.reports.xml.dir}" if="testcase"/>
		 	<!-- ant test -Dtestcase=<fully qualified classname> -->
		 	<!--
		 	  <sysproperty key="docs.dir" value="${test.dir}/org"/>  
		 	  Manche Tests benötigen System Properties, diese können im Test umdefiniert werden.
		 	-->
		 	<batchtest todir="${war.reports.xml.dir}" unless="testcase">
				<fileset dir="${war.test.dir}">
					<include name="**/*Test.java" />
				</fileset>
			</batchtest>
		</junit>

		<junitreport todir="${war.reports.xml.dir}">
			<fileset dir="${war.reports.xml.dir}">
				<include name="TEST-*.xml" />
			</fileset>
			<report format="frames" todir="${war.reports.html.dir}" />
		</junitreport>
		<fail message="Tests failed. Check log and/or reports."	if="test.failed"/>
	</target>

	<target name="clean" description="Remove all files created by the build/test process.">
		<delete dir="${war.classes.dir}" />
		<delete dir="${war.reports.dir}" />

		<delete file="${war.cobdata}" />
		<delete dir="${war.inst.dir}" />
		<delete dir="${war.cob.rep}" />
	</target>

	<!-- =================================
		          target: Cobertura
		 ================================= -->
	<taskdef classpathref="junit.classpath" resource="tasks.properties" />
	<target name="cob-instr" depends="compile">
		<cobertura-instrument todir="${war.inst.dir}" datafile="${war.cobdata}">
		    <fileset dir="${war.classes.dir}">
		        <include name="**/*.class" />
		        <exclude name="**/*Test.class" />
		        <exclude name="**/*Suite.class" />
		    </fileset>
		</cobertura-instrument>
	</target>

	<target name="cob-rep" depends="cob-instr, test" description="erzeugt Cobertura Reports">
		<cobertura-report format="html" datafile="${war.cobdata}" destdir="${war.cob.rep.html}" srcdir="${war.src.dir}" />
		<cobertura-report format="xml" datafile="${war.cobdata}" destdir="${war.cob.rep.xml}" srcdir="${war.src.dir}" />
	</target>
    <!--

    There exist several targets which are by default empty and which can be 
    used for execution of your tasks. These targets are usually executed 
    before and after some main targets. They are: 

      -pre-init:                 called before initialization of project properties 
      -post-init:                called after initialization of project properties 
      -pre-compile:              called before javac compilation 
      -post-compile:             called after javac compilation 
      -pre-compile-single:       called before javac compilation of single file
      -post-compile-single:      called after javac compilation of single file
      -pre-compile-test:         called before javac compilation of JUnit tests
      -post-compile-test:        called after javac compilation of JUnit tests
      -pre-compile-test-single:  called before javac compilation of single JUnit test
      -post-compile-test-single: called after javac compilation of single JUunit test
      -pre-dist:                 called before archive building 
      -post-dist:                called after archive building 
      -post-clean:               called after cleaning build products 
      -pre-run-deploy:           called before deploying
      -post-run-deploy:          called after deploying

    Example of pluging an obfuscator after the compilation could look like 

        <target name="-post-compile">
            <obfuscate>
                <fileset dir="${build.classes.dir}"/>
            </obfuscate>
        </target>

    For list of available properties check the imported 
    nbproject/build-impl.xml file. 


    Other way how to customize the build is by overriding existing main targets.
    The target of interest are: 

      init-macrodef-javac:    defines macro for javac compilation
      init-macrodef-junit:   defines macro for junit execution
      init-macrodef-debug:    defines macro for class debugging
      do-dist:                archive building
      run:                    execution of project 
      javadoc-build:          javadoc generation 

    Example of overriding the target for project execution could look like 

        <target name="run" depends="<PROJNAME>-impl.jar">
            <exec dir="bin" executable="launcher.exe">
                <arg file="${dist.jar}"/>
            </exec>
        </target>

    Notice that overridden target depends on jar target and not only on 
    compile target as regular run target does. Again, for list of available 
    properties which you can use check the target you are overriding in 
    nbproject/build-impl.xml file. 

    -->
</project>
